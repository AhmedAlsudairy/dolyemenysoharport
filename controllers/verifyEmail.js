export const verifyEmail= async ()=>{
const emailToken=req.body.emailToken;
!emailToken && return res.

















































































































































}